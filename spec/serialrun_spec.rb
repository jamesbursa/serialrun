require "spec_helper"

describe SerialRun do
  it "has a version number" do
    expect(SerialRun::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
